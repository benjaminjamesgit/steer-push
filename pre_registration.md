# steer-push — Pre-Registration

## 0. Why this project exists
The Recursive/Adaptive Coherence program's measurable claims reduce to existing constructs. Its defensible frontier is a naturalist/physical semiotics: when does a physical difference become a SIGN, below life? An adversarial prior-art audit found a real unoccupied slot. Persistence-steering as a criterion is prior art: Friston's free-energy principle owns it and runs it below life by construction, but as a deliberate tautology; Deacon's teleodynamics owns it at self-maintenance. The SEMIOTIC identification below life is unoccupied: the persistence/function owners (Friston, Kauffman, assembly theory) disclaim the sign; the sign owners (biosemiotics, Pattee, Sebeok) refuse to go below life. The slot is empty because filling it needs a criterion separating a persistence-steering difference that is a SIGN from one that is merely a CAUSE — which neither the field nor the corpus currently has. This project tries to build it.

## 1. Target and bar
THE CRITERION: a function C(D, S, dyn) -> graded magnitude, deciding whether a physical difference D acting on system S under dynamics `dyn` STEERS S (selection) or merely PUSHES it (causation).
THE BAR (fixed; do not move): the criterion is a genuine contribution IFF there exists at least one concrete PRE-LIFE, NON-SELF-MAINTAINING system that C labels STEER, that Friston's FEP does NOT distinguish from a mere persister (FEP-flat), and that Deacon's teleodynamics labels PUSH (below the self-maintenance gate). If no such system survives — or every candidate dissolves into "a system that benefits" or "a boundary condition" — we CONCEDE: renaming.

## 2. Objects (thin, so nothing biological/semiotic is smuggled in)
- System S: a bare dynamical system — a state space plus an evolution rule. No boundary, no self-maintenance, no observer. Some states persist (attractors); some do not (transients).
- Difference D: always a contrast between >=2 alternative conditions {d, d'} (perturbation, input, or parameter), never a single absolute value. (A difference is a relation between alternatives — Bateson.)
- Response R: the RETAINED STATE (the attractor S settles into) as a function of which alternative obtained: R(d) vs R(d'). (Trajectory-based R deferred to later work.)
- Output: graded (near zero = push, large = steer); STEER/PUSH labels thresholded only for the matched-pair test.

## 3. Positive core (steer vs push)
PUSH: D supplies the outcome — the response is a forced function of D and scales with it (D -> 0 implies response -> 0).
STEER: D selects among outcomes S already affords — D picks which retained state; the states and their magnitudes belong to S, not D.
A steer requires what a push does not:
  (1) Afforded alternatives: under fixed gross conditions, S has >=2 retained states available (multistability).
  (2) Trigger, not drive: D sets WHICH alternative, not HOW BIG. Diagnostic: an arbitrarily small D still flips the outcome (a threshold/sensitivity), so the response does not scale to zero with D the way a push does.
Minimal pair (pure physics, no life): PUSH = a rock on a slope (one outcome; scales with the nudge). STEER = a ball on a ridge (two afforded valleys; a vanishing nudge selects which; outcome size is the ridge's, not the nudge's).

## 4. Genus and species (the "recursive choice" refinement)
A "choice" is chooser-free: an irreversible LOCAL creation of information. The cut is SOURCE, not amount. A PUSH transmits the input's information (the choice was made upstream; the system is a conduit). A STEER creates information locally (the retained state carries a bit the input did not determine; S's afforded alternatives supplied it, D only selected which). Irreversibility makes the created bit stick — it is the retained state.
  - GENUS (recursive choice): a local irreversible selection. Chooser-free. NOTE: the genus includes pure noise (random symmetry-breaking creates a local bit but is coupled to no specific D).
  - SPECIES (steer / sign): a choice whose locally-created bit is RELIABLY COUPLED to a difference (the same d reliably selects the same R; different d's select different R's).
The criterion's job is to carve the species out of the genus.

## 5. Recursion-as-reliability (where reliability comes from with no chooser)
Mechanism: a choice leaves an irreversible record; the record becomes part of the SELECTOR (the structure fixing the d->R mapping); if the record persists, it re-imposes the same selection. Reliability of the coupling = PERSISTENCE OF THE SELECTOR ("memory without a mind": a record that is itself a stable selector).
MAKE-OR-BREAK FORK — why does a selector persist?
  (A) It persists because the coupling keeps a SYSTEM alive (a beneficiary maintains the record) => re-imports Deacon's self-maintenance gate => steer = teleodynamics => COLLAPSE.
  (B) It persists because it is simply a stable configuration (template, constraint, low-energy structure) that re-imposes its selection whether or not anything benefits => no beneficiary => chooser-free AND gate-free => the CONTRIBUTION.
The project stands iff (B) is genuinely distinct from (A): a selector that persists for purely structural reasons and reliably re-selects with NOTHING benefiting. Candidate type (to build and test): a pre-life persistent selector — e.g., a templating structure or a standing dissipative channel that reliably re-imposes a selection with no beneficiary. This threads between Pattee (persistent rate-independent selector, but gated at self-replication) and assembly theory (persistence-as-memory, but declines the sign).

## 6. Roadmap
- Step 5 (next): pre-register and build the simplest (B) candidate plus its matched PUSH control; pre-register the operational measures (afforded-alternatives, trigger-not-drive, local-information-creation, reliability/selector-persistence) and the predicted verdicts BEFORE running.
- Step 6: run; adjudicate against the bar in Section 1, with the FEP-flatness check and the Deacon-PUSH check made explicit, not assumed. Concede or claim.

## Standing reading (hold weak points as weak)
Nearest renaming traps: (i) the criterion labels all symmetry-breaking STEER (vacuity / Friston tautology) — guard with matched push controls and the reliability requirement; (ii) the (B) candidate persists because something benefits, (A) in disguise — guard by exhibiting the no-beneficiary structure explicitly; (iii) "persistent selector" reduces to "boundary condition / constraint" with no semiotic surplus — the deepest trap; the bar in Section 1 is the test.
